# Aria Character & Animation Implementation Guide

## 概要

Aria（AI戦略パートナー）に立ち絵とアニメーション機能を追加しました。これにより、ユーザーとの情緒的つながりが生まれ、継続率の向上が期待できます。

## 実装内容

### 1. 立ち絵コンポーネント (`AriaCharacter.tsx`)

**機能**:
- 感情状態に応じた表情・背景色の変化
- 微呼吸アニメーション（常時動作）
- 瞬きアニメーション（3-7秒間隔）
- 話している時の口元アニメーション
- 考え中アニメーション（首をかしげる）
- 微細なランダム行動（伸び、見回す、体重移動など）
- 元気レベルインジケーター（ゲーミフィケーション）
- タスク完了時の花火エフェクト（元気レベル80以上）

**感情状態**:
- `motivated`: 前向き（✨表示）
- `anxious`: 不安・焦り（💭表示）
- `tired`: モチベーション低下（😌表示）
- `confused`: 混乱・不明（質問モード）
- `neutral`: 中立

### 2. アニメーションコントローラー (`AriaAnimationController.tsx`)

**機能**:
- ユーザーメッセージから感情状態を自動検出
- 感情状態に応じた心理学モードの決定
- 元気レベルの管理（ゲーミフィケーション）
- 時間経過による元気レベルの自然な減衰

**感情検出キーワード**:
- モチベーション低下: 「疲れた」「やる気」「できない」「無理」など
- 不安・焦り: 「不安」「焦る」「心配」「どうしよう」など
- 前向き: 「やる」「進める」「頑張る」「できる」など
- 混乱: 「わからない」「どうすれば」「教えて」など

### 3. AriaChat統合

**変更点**:
- 2カラムレイアウト（左：立ち絵、右：チャット）
- 立ち絵とチャットの状態を同期
- メッセージ送信時に元気レベルを増加

## 使用方法

### 基本的な使用

```tsx
import { AriaChat } from '@/feature/common/AriaChat';

<AriaChat onStartAnalysis={handleAnalysis} showUserAvatar={true} />
```

立ち絵は自動的に表示され、ユーザーのメッセージに応じて感情状態が変化します。

### カスタマイズ

立ち絵の状態を直接制御する場合:

```tsx
import { AriaCharacter, type CharacterState } from '@/feature/common/AriaCharacter';

const state: CharacterState = {
  emotion: 'motivated',
  psychologyMode: 'strategic',
  isThinking: false,
  isSpeaking: true,
  energyLevel: 75,
  lastInteraction: new Date(),
};

<AriaCharacter state={state} className="w-full h-full" />
```

## アニメーション詳細

### 常時動作
- **微呼吸**: 上下に3px移動（3秒周期）
- **瞬き**: 3-7秒間隔で自動的に瞬き

### 状態に応じたアニメーション
- **考え中**: 首を左右に2度傾ける（2秒周期）
- **話している**: 口元が上下に動く（0.5秒周期）
- **ランダム行動**: 5-15秒間隔で以下のいずれかが発生
  - 伸び（stretch）
  - 見回す（lookAround）
  - 体重移動（weightShift）

### 感情状態に応じた視覚効果
- **背景グラデーション**: 感情状態に応じて色が変化
- **表情インジケーター**: 絵文字で感情を表現
- **スケール**: 感情状態に応じてキャラクターのサイズが微調整

## ゲーミフィケーション

### 元気レベルシステム
- 初期値: 50
- メッセージ送信: +2
- タスク完了: +5（手動で呼び出し可能）
- 時間経過: 1分ごとに-0.1（最低30まで）

### 花火エフェクト
元気レベルが80以上になると、タスク完了時に花火エフェクトが表示されます。

## パフォーマンス最適化

- CSSアニメーションを使用（GPU加速）
- ランダム行動は適度な間隔で実行（5-15秒）
- 不要な再レンダリングを防止

## 注意点

### 軽量性の維持
- アニメーションは控えめに（主役は「行動」）
- キャラクターの性格はニュートラル8割、キャラ性2割
- 会話に割り込むような演出は避ける

### レスポンシブ対応
- 大画面（lg以上）: 立ち絵を表示
- 小画面: 立ち絵を非表示（チャットのみ）

## 今後の拡張案

1. **Live2D統合**: より滑らかなアニメーション
2. **音声同期**: 音声合成時の口パク同期
3. **ペルソナ別キャラクター**: ユーザーの業種に応じたキャラクター変化
4. **カスタマイズ機能**: ユーザーがキャラクターの見た目をカスタマイズ

## 参考資料

- 行動経済学: ナッジ理論、パラソーシャル効果
- 心理学: 認知行動療法、感情状態の検出
- UI/UX: ゲーミフィケーション、継続率向上のベストプラクティス

